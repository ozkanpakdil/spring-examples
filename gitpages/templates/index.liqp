tepeindex
{{tt}}

  {% for p in presentations %}
    <div class="card mb-3 shadow-sm rounded">
              <div class="card-header">
                  <h5 class="card-title">{{ p.title }} - {{ p.speakerName }}</h5>
              </div>
              <div class="card-body">
                  {{ p.summary }}
              </div>
          </div>
  {% endfor %}